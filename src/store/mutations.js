export const mutations = {
  setPlayers(state, players) {
    state.players = players;
  },
  setShowModal(state, showModal) {
    state.showModal = showModal;
  },
  setSelectPlayer(state, player) {
    state.player = player;
  },
};
